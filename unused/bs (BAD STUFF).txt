            if (cword) {
                document.getElementById('test').innerHTML = 
                '<p> The word is: ' + cword + '</p>';
            } else {
                document.getElementById('test').innerHTML = 
                '<p> The word is: NO FAST FREDBEAR FOUND</p>';
            }

<style
@font-face {
    font-family: LFI;
    src: url(fonts/LFI.ttf);
}

h1 {
    font-family: LFI;
}

<p id="test">this is the game</p>

sessionStorage.setItem('Row', 1);
sessionStorage.setItem('Column', 1);

var Column = sessionStorage.getItem('Column');
var Row = sessionStorage.getItem('Row');


console.log(Row)
console.log(Column)

if (result == true) {
    alert("true")
 else {
    alert("false")
}

ROW CHANGING LOGIC 
 if (Column == 5) {
                    if (document.getElementById(`R${Row}C5`).innerHTML != '') {
                        if (Row != 6) {
                            console.log("done")
                            Row += 1
                            Column = 1
                        }
                    }
                }

  console.log("yes")
                        if (cword.includes(word[i])) {
                            let currentleter = word[i]
                            let resultword = cword.indexOf(currentleter)
                            if (cword[resultword] == currentleter) {
                                console.log("OI OI")
                                document.getElementById(`R${Row}C${resultword + 1}`).classList.add("correctletter")
                            }
                            
                        }

                        if (word[i] in duplicates) {
                                for(var key in duplicates) {
                                    var item = duplicates[key];
                                    console.log(":)")
                                    console.log("_____")
                                    console.log(item)
                                }
                            }