function backspaceKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "Backspace",
        code: "Backspace"
    });

    document.dispatchEvent(letter);
}

function enterKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "Enter",
        code: "Enter"
    });

    document.dispatchEvent(letter);
}

function AKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "A",
        code: "KeyA"
    });

    document.dispatchEvent(letter);
}

function BKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "B",
        code: "KeyB"
    });

    document.dispatchEvent(letter);
}

function CKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "C",
        code: "KeyC"
    });

    document.dispatchEvent(letter);
}

function DKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "D",
        code: "KeyD"
    });

    document.dispatchEvent(letter);
}

function EKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "E",
        code: "KeyE"
    });

    document.dispatchEvent(letter);
}

function FKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "F",
        code: "KeyF"
    });

    document.dispatchEvent(letter);
}

function GKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "G",
        code: "KeyG"
    });

    document.dispatchEvent(letter);
}

function HKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "H",
        code: "KeyH"
    });

    document.dispatchEvent(letter);
}

function IKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "I",
        code: "KeyI"
    });

    document.dispatchEvent(letter);
}

function JKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "J",
        code: "KeyJ"
    });

    document.dispatchEvent(letter);
}

function KKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "K",
        code: "KeyK"
    });

    document.dispatchEvent(letter);
}

function LKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "L",
        code: "KeyL"
    });

    document.dispatchEvent(letter);
}

function MKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "M",
        code: "KeyM"
    });

    document.dispatchEvent(letter);
}

function NKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "N",
        code: "KeyN"
    });

    document.dispatchEvent(letter);
}

function OKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "O",
        code: "KeyO"
    });

    document.dispatchEvent(letter);
}

function PKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "P",
        code: "KeyP"
    });

    document.dispatchEvent(letter);
}

function QKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "Q",
        code: "KeyQ"
    });

    document.dispatchEvent(letter);
}

function RKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "R",
        code: "KeyR"
    });

    document.dispatchEvent(letter);
}

function SKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "S",
        code: "KeyS"
    });

    document.dispatchEvent(letter);
}

function TKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "T",
        code: "KeyT"
    });

    document.dispatchEvent(letter);
}

function UKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "U",
        code: "KeyU"
    });

    document.dispatchEvent(letter);
}

function VKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "V",
        code: "KeyV"
    });

    document.dispatchEvent(letter);
}

function WKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "W",
        code: "KeyW"
    });

    document.dispatchEvent(letter);
}

function XKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "X",
        code: "KeyX"
    });

    document.dispatchEvent(letter);
}

function YKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "Y",
        code: "KeyY"
    });

    document.dispatchEvent(letter);
}

function ZKey() {
    const letter = new KeyboardEvent("keydown", {
        key: "Z",
        code: "KeyZ"
    });

    document.dispatchEvent(letter);
}